<!doctype html>
<html>
<head>
  <title>Salon Recomendations</title>
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
  <link rel="stylesheet" href="style.css">
  <link href="https://fonts.googleapis.com/css?family=Merienda&display=swap" rel="stylesheet">
</head>
<body>
  <div class="container">

    <div class="page-header text-center">
      <h1> Salon Recomendations</h1>
      <a href="/logout" class="btn btn-default btn-sm">Logout</a>
    </div>

    <div class="row">
      <!-- LOCAL INFORMATION -->
      <!-- <div class="col-sm-6">
      <div class="well">
      <h3><span class="fa fa-user"></span> Local</h3>

      <% if (user.local.email) { %>
      <p>
      <strong>id</strong>: <%= user._id %><br>
      <strong>email</strong>: <%= user.local.email %><br>
      <strong>password</strong>: <%= user.local.password %>
    </p>

    <a href="/unlink/local" class="btn btn-default">Unlink</a>
    <% } else { %>
    <a href="/connect/local" class="btn btn-default">Connect Local</a>
    <% } %>

  </div>
</div> -->
<div class="col-sm-6">
  <h3>Salons</h3>
  <ul class="salons">
    <% for(var i=0; i< salonCollection.length; i++) {%>
      <% if (user.local.email == salonCollection[i].userName) {%>
        <li class="salon">
          <span><%= salonCollection[i].salonName %></span>
          <span><%= salonCollection[i].location %></span>
          <span><%= salonCollection[i].thumbUp %></span>
          <span><%= salonCollection[i].feeling %></span>

          <span><i class="fa fa-thumbs-up" aria-hidden="true"></i></span>
          <!-- <span><i class="fa fa-thumbs-down" aria-hidden="true"></i></span> -->
          <span><i class="fa fa-trash" aria-hidden="true"></i></span>

        </li>
        <% } %>
        <% } %>
      </ul>

      <h2>Add a salon:</h2>
      <% if (user.local.email) { %>
        <form action="/newSalon" method="POST">
          <input readonly type="text" value="<%= user.local.email %>" name="userName">
          <input type="text" placeholder="Enter a salon name" name="salonName">
          <input type="text" placeholder="Enter salon location" name="location">
          <button type="submit">Submit</button>
        </form>
      </div>
      <% } %>
      <a href="/fav"><button type="button" name="button">Go To Favorites</button></a>
    </div>


    <script src="main.js"></script>
    <script src="https://kit.fontawesome.com/077191d827.js" crossorigin="anonymous"></script>

  </body>
  </html>
